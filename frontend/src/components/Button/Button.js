import React from "react";
import "./Button.scss";

const Button = () => {
  return (
    <button type="submit" className="culture-form__submit btn">
      Submit
    </button>
  );
};

export default Button;
